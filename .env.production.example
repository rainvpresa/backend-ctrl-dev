# Example production environment variables (copy to `.env.production` and set real values)
DJANGO_SECRET_KEY=4S2g9kDqz7YvXcW1pLmT8hN5aR0uF3jBqVzE6yP0sGhAq
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=your.domain.com

# Database URL for dj-database-url
# Example: postgres://dbuser:dbpassword@db:5432/agham_db
DATABASE_URL=postgres://postgres:postgres@db:5432/agham_db

# Postgres container initialization vars (used by the official Postgres image)
# These ensure the running database is created with the expected name and user.
POSTGRES_DB=agham_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Redis URL (for celery/session/cache)
REDIS_URL=redis://redis:6379/0

# Email (SMTP) example
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-smtp-user
EMAIL_HOST_PASSWORD=your-smtp-password

# Site ID for django-allauth
DJANGO_SITE_ID=1
